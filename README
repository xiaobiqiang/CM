节点通信规则：
同一子域内的不同节点可以互相通信
一个子域主可以与其他子域主互相通信
一个不是子域主的节点可以通过子域主转发来与不同子域的子域主通信。(一次转发)
一个不是子域主的节点可以通过两个子域主转发来与不同子域内不是子域主的节点通信。(二次转发)

一个节点可以获取到同一子域内任意一个节点的信息以及子域信息
子域主可以获取到任意一个子域主的节点信息。

子域主的信息几乎相当于一个子域的信息，子域主代表这个子域与外界进行通信。
抛开与外界通信不谈，子域内的节点是互相平等的。
